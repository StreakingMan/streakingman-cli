/**
* stylelint config
* @ref https://stylelint.io/
* @desc generated at <%- generatedAt %> by streakingman-cli@<%- version %>
*/

module.exports = {
    "extends": [
        "stylelint-config-standard-scss",
        <% if (prettier) { %>  "stylelint-config-prettier-scss" <% } %>
    ]
}
